site_name: Otterdog configuration @ eclipse-mpc
site_description: Otterdog configuration @ eclipse-mpc
strict: false

site_url: https://eclipse-mpc.github.io/.eclipsefdn/

repo_name: eclipse-mpc/.eclipsefdn
repo_url: https://github.com/eclipse-mpc/.eclipsefdn/

docs_dir: ./docs
site_dir: ./_site

theme:
  name: 'material'
  custom_dir: docs/overrides
  features:
    - navigation.tabs

markdown_extensions:
  - pymdownx.superfences
  - pymdownx.tabbed:
      alternate_style: true
  - attr_list
  - pymdownx.emoji:
      emoji_index: !!python/name:materialx.emoji.twemoji
      emoji_generator: !!python/name:materialx.emoji.to_svg

plugins:
  - macros

nav:
  - Overview: index.md

watch: []

extra:
  org_id: eclipse-mpc

extra_css:
  - stylesheets/extra.css
